node_modules
out

# Vale's style packages, vendored by `vale sync` and not ours to format.
styles/Google
styles/proselint

# Prettier is here for Markdown and nothing else — it is the one file type Biome
# does not format. Everything Biome does format is listed here so that a bare
# `prettier --write .` cannot quietly take the JavaScript off it and start a
# formatter war in the pre-commit hook.
*.js
*.jsx
*.ts
*.tsx
*.json
*.jsonc
*.css
